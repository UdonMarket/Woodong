<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="model.BoardListImpl">
	
	<select id="selectOrder" resultType="int">
		SELECT count(*) FROM boardlist WHERE location=#{0}
	</select>

	<insert id="createboard" parameterType="model.BoardListVO"> 
		INSERT INTO boardlist (bname, location, locationname, requestname, boardorder) VALUES (#{bname}, #{location}, #{locationname}, #{requestname}, #{boardorder})
	</insert>
	
	<select id="selectBoard" resultType="model.BoardListVO">
		SELECT * FROM boardlist 
		<if test="!(_parameter.equals('../admin/addBoard.woo'))">
			WHERE location=#{0}
		</if>
		ORDER BY location, boardorder
	</select>
	
	<update id="editboard" parameterType="model.BoardListVO">
		UPDATE boardlist SET bname=${bname}, location=${location}, requestname=${requestname}, locationname=${locationname}, type=${type} 
	</update>
</mapper>