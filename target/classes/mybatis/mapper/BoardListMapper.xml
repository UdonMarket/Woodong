<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="model.BoardListImpl">
	
	<select id="selectOrder" resultType="int">
		SELECT count(*) FROM boardlist WHERE location=#{0}
	</select>

	<insert id="createboard" parameterType="model.BoardListDTO"> 
		INSERT INTO boardlist (bname, location, locationname, requestname, borderorder) 
				VALUES (#{bname}, #{location}, #{locationname} #{requestname}, #{boardorder})
	</insert>
	
	<select id="selectBoard" resultType="model.BoardListDTO">
		SELECT * FROM boardlist 
		<if test="!(_parameter.equals('../admin/addBoard.woo'))">
			WHERE location=#{0}
		</if>
		ORDER BY location, boardorder
	</select>
	
	<!--bname 검색  -->
	<select id="selectBname" resultType="model.BoardListDTO">
		SELECT * FROM boardlist
	</select>
</mapper>